<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CO2Bakalauras.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <StackLayout Padding="40" VerticalOptions="Center">
            <Image Source="xamarin_logo.png" VerticalOptions="Center" HeightRequest="64" />

            <Entry Placeholder="Prisijungimo vardas" Text="{Binding Login}" TextColor="White" PlaceholderColor="Gray"/>
            <Entry Placeholder="Slaptažodis" IsPassword="True" Text="{Binding Psw}" TextColor="White" PlaceholderColor="Gray"/>
            <Button 
                VerticalOptions="Center" 
                Text="Prisijungti" 
                Command="{Binding LoginCommand}"
                BorderColor="ForestGreen"
                BorderWidth="2"
                CornerRadius="50"
            ></Button>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>